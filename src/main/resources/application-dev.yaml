spring:
  application:
    name: compilerjava
  datasource:
    url:  ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3307/grader}
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  servlet:
    context-path: /java-compiler-service
  port: 5000